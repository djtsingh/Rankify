{
  "name": "rankify-api",
  "version": "1.0.0",
  "description": "Rankify Azure Functions API",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "npx prisma generate && tsc",
    "watch": "tsc -w",
    "clean": "rimraf functions lib dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "@prisma/client": "5.22.0",
    "dotenv": "^17.2.3",
    "prisma": "5.22.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "azure-functions-core-tools": "^4.x",
    "rimraf": "^5.0.0"
  }
}